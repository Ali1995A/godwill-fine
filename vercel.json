{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "npm install",
        "buildCommand": "npm run build"
      }
    }
  ],
  "functions": {
    "api/ai-analysis.js": {
      "maxDuration": 60
    }
  }
}